# Python
__pycache__/
*.py[cod]
*.egg-info/
.eggs/
build/
dist/
.venv/
venv/
env/

# Tooling
.pytest_cache/
.ruff_cache/
.ipynb_checkpoints/

# Generated datasets & run artifacts (regenerable; some may contain scrubbed-but-sensitive data)
# Leading slash anchors to the repo root so we DON'T ignore the source package logsub/data/.
/data/
/runs/
results/
*.jsonl

# Models / large weights — never commit
models/
*.safetensors
*.gguf
*.bin

# Local config / secrets
.env
*.local

# claude
.claude/
